#include <stdio.h>
#include <string.h>
/**
 *main - Main function
 *Return: 0 Always (success)
 */

int main(void)
{
char letter;

for (letter = 'a'; letter <= 'z'; letter++)
	putchar(letter);

putchar('\n');

    return (0);
}

